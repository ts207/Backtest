families:
  # --- Focused signal families (mean_reversion only at 60m to minimize BH FDR inflated pool) ---
  funding_normalization:
    templates:
      - mean_reversion
    horizons:
      - 60m
    max_candidates_per_run: 500
  funding_extreme_reversal_window:
    templates:
      - mean_reversion
    horizons:
      - 60m
    max_candidates_per_run: 500
  directional_exhaustion_after_forced_flow:
    templates:
      - mean_reversion
    horizons:
      - 60m
    max_candidates_per_run: 500
  vol_shock_relaxation:
    templates:
      - mean_reversion
    horizons:
      - 60m
    max_candidates_per_run: 500
  vol_aftershock_window:
    templates:
      - mean_reversion
    horizons:
      - 60m
    max_candidates_per_run: 500
  liquidity_vacuum:
    templates:
      - mean_reversion
      - continuation
    horizons:
      - 5m
      - 15m
      - 60m
    max_candidates_per_run: 1000
  funding_persistence_window:
    templates:
      - mean_reversion
      - continuation
    horizons:
      - 5m
      - 60m
    max_candidates_per_run: 500
  oi_flush:
    templates:
      - mean_reversion
    horizons:
      - 5m
      - 60m
    max_candidates_per_run: 500
  vol_aftershock_window:
    templates:
      - mean_reversion
      - continuation
      - trend_continuation
      - liquidity_reversion_v2
    horizons:
      - 5m
      - 15m
      - 60m
    max_candidates_per_run: 1000
  range_compression_breakout_window:
    templates:
      - mean_reversion
      - continuation
      - liquidity_reversion_v2
    horizons:
      - 5m
      - 15m
      - 60m
    max_candidates_per_run: 1000
  liquidity_absence_window:
    templates:
      - mean_reversion
      - continuation
      - liquidity_reversion_v2
    horizons:
      - 5m
      - 15m
      - 60m
    max_candidates_per_run: 1000

budget:
  global_max_candidates: 10000
  global_max_families: 10
